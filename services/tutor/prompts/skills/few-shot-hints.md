# Skill: few-shot shape (hints)

Match this output shape exactly (language follows the course/learner):

Example (Russian course):
- Уточните, какие столбцы нужны в результате, а какие можно отбросить.
- Проверьте, указали ли вы список столбцов через запятую после SELECT.
- Сверьте имена столбцов с схемой на текущей странице — не придумывайте поля.

Example (English course):
- Clarify which columns belong in the result set.
- Check whether column names after SELECT are comma-separated.
- Match identifiers to the schema on the current page — do not invent fields.

Still: exactly 2 or 3 bullets, no preamble, no full solutions.
